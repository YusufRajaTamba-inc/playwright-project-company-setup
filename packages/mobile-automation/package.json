{
  "name": "@automation/mobile",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "lint": "eslint \"**/*.js\" --fix --no-error-on-unmatched-pattern",
    "test": "echo \"Mobile test runner not set yet\" && exit 0"
  }
}
